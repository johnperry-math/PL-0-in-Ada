VAR A, B;

PROCEDURE TWO;
BEGIN
  A := THREAD;
END;

PROCEDURE ONE;
VAR X;
BEGIN
  CALL TWO;
  X := THREAD;
  WRITELN(A, B, X);
END;

BEGIN
  COBEGIN
    CALL ONE;
    B := A + THREAD;
  COEND;
  WRITELN(A, B);
END;
